# 2026-03-26 (Day2)

## What I did

- Fixed repository structure and ensured all directories are created inside the project root
- Organized project folders: scripts/, embedded/arduino/, docs/, etc.
- Created serial communication module structure:
  - scripts/serial/
  - embedded/arduino/basic_serial/

- Implemented Python-based serial sender:
  - Established serial connection using pyserial
  - Designed a loop to send messages periodically
  - Added logging ([INFO], [SEND])
  - Implemented exception handling and safe shutdown

- Implemented Arduino serial receiver:
  - Initialized serial communication with Serial.begin(115200)
  - Used Serial.available() to detect incoming data
  - Parsed incoming messages using readStringUntil('\n')
  - Printed received messages for debugging

- Built a minimal communication pipeline:

  Python → Serial (USB) → Arduino → Serial Monitor Output

## Key Understanding

- Understood the role separation in robot systems:
  - Python (ROS2 side) acts as the high-level command sender
  - Arduino acts as the low-level hardware controller

- Learned that serial communication is the bridge between high-level logic and physical execution

- Realized that:
  This is not just writing scripts, but building a cross-layer system (software ↔ hardware)

## Issues / Fix

- Mistakenly created folders outside the repository root → fixed by navigating correctly using cd
- Incorrect function name readStringUntill → corrected to readStringUntil
- Used incorrect brackets in Python (main（）) → fixed to main()

## Result

- Successfully prepared both sides of serial communication (sender + receiver)
- Established a clean and scalable project structure
- Ready for hardware testing once Arduino board is available

## Next Step

- Connect Arduino Uno R3
- Upload receiver code via Arduino IDE
- Identify correct serial port
- Run Python sender and verify end-to-end communication